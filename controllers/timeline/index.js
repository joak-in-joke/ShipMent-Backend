var getTimeline = require('./getTimeline')
var createComment = require('./createComment')
var deleteComment = require('./deleteComment')
var endTimeline = require('./endTimeline')

module.exports = {
    getTimeline,
    createComment,
    deleteComment,
    endTimeline
}